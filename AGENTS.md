# OpenJWC Go 开发规约

本规范适用于本目录及其所有子目录。子目录中的 `AGENTS.md` 可以补充更具体的规则；发生冲突时，以距离目标文件最近的规则为准。

## 工具链与常用命令

- Go 版本以 `go.mod` 为准，不得擅自降低或提高最低版本。
- 优先使用标准库；新增第三方依赖前必须说明标准库为何不足，并检查维护状态、许可证和间接依赖。
- 修改 Go 代码后运行 `gofmt`，提交前必须运行 `go vet ./...` 和 `go test ./...`。
- 可使用 `make check` 依次执行格式化、静态检查和测试。
- 不得手工修改生成代码；应修改来源文件并重新运行生成器。

## 架构边界

- `cmd/` 只负责进程入口、配置加载、依赖组装、信号处理和生命周期，不放业务规则。
- `internal/domain/` 保存领域实体、值对象和由使用方需要的最小接口，不依赖 HTTP、SQL、日志或第三方 SDK。
- `internal/transport/` 负责协议解析、输入校验、DTO 转换和状态码映射，不直接编写 SQL。
- `internal/app/` 是依赖组装边界，不得成为全局服务定位器。
- `migrations/` 保存版本化数据库迁移；已经进入共享环境的迁移不得改写，只能新增迁移修正。
- 外部 API 契约以 `api/openapi/` 为准。修改行为时同步更新契约和兼容测试。
- 接口由消费方定义并保持精简。不要为了“可能有用”提前创建空包、空接口或通用抽象。
- 依赖方向必须指向领域层；领域层不得导入传输层或基础设施层。

## 文件与代码规模

- 一个手写 `.go` 文件应围绕一个明确职责组织，通常只定义 1～3 个主要类型；紧密关联的输入类型、枚举和小接口可以放在同一文件。
- 手写 `.go` 文件应尽量控制在 200 行内，最多不得超过 300 行。超过上限时按职责拆包或拆文件，不得用无意义的辅助函数规避限制。
- 生成代码、迁移和静态数据不受行数限制，但必须保持来源可追踪。
- 函数应短小且只完成一个层级的工作。复杂分支优先通过提前返回降低嵌套。
- 包名使用简短的单数英文，不使用 `common`、`utils`、`helpers` 等含义模糊的名称。
- 接收者名称使用类型含义明确的短名称，不使用 `this`、`self` 或无意义单字母。

## 注释与命名

- 所有人工撰写的代码注释必须使用中文；标识符、协议字段和外部规范要求的文本仍使用英文。
- 禁止行尾注释、语句内部注释、无意义 banner、重复代码含义的注释和大段被注释掉的代码。
- 每个导出的类型、常量、变量、函数和方法应有至多一段简洁中文注释，并以被说明的标识符开头；需要包说明时使用独立的 `doc.go`。
- 文件头只允许许可证、生成声明或确有必要的包级说明，合计不得超过 5 行。
- 函数内部仅在约束、算法或外部兼容行为无法由命名表达时添加一行注释。
- 测试函数可以使用多行中文注释解释场景和断言，但仍禁止行尾注释和语句内部注释。
- 错误、日志和用户可见文本应明确说明上下文，不使用“出错了”一类模糊表达。

## 类型与领域建模

- 所有函数和方法必须使用明确的参数类型和返回类型；不得使用 `any` 绕过可表达的领域类型。
- 面向业务的领域实体不得要求调用方使用结构体字面量直接构造。字段应保持私有，并提供能表达语义且校验不变量的构造函数，例如 `notice.New`、`access.NewAPIKey` 或后续的 `RestoreNotice`。
- 可能失败的构造函数必须返回错误。不得创建后再依赖调用方补齐必填字段。
- 状态变换使用名称明确的方法，例如 `BindDevice`、`Review`。优先返回新值；确需原地修改时必须说明并发和所有权边界。
- HTTP DTO、配置项和仅用于参数聚合的输入结构可以使用结构体字面量，但不得把它们当作领域实体。
- 枚举使用自定义类型和具名常量，并在输入边界拒绝未知值。
- 时间在领域层使用 `time.Time`，持久化和 API 边界明确时区与格式；默认统一为 UTC 和 RFC 3339。
- 切片、映射、指针和字节数组跨领域边界时应考虑复制，禁止无意泄露可变内部状态。
- 金额、计数、配额和分页参数必须校验范围，不得依赖数据库报错代替领域校验。

## 错误、上下文与并发

- 预期失败返回 `error`，不得用 panic 处理用户输入、网络失败或数据库失败。
- 包装错误使用 `%w` 保留错误链；调用方使用 `errors.Is` 或 `errors.As`，不得依赖字符串比较。
- `context.Context` 作为需要取消或超时操作的第一个参数，不得保存到结构体字段，也不得传入 `nil`。
- 启动的 goroutine 必须有明确的停止条件、所有者和错误处理路径；禁止无法回收的后台任务。
- 共享状态优先通过不可变值或消息传递管理；使用锁时将锁与被保护数据放在同一类型中。
- 日志使用 `log/slog` 及结构化字段。严禁记录 API Key、JWT、密码、Authorization 头或完整敏感请求体。

## HTTP、数据库与安全

- 传输层必须限制请求体大小、校验路径和查询参数，并为外部调用设置超时。
- 不直接向客户端暴露领域实体、数据库行或内部错误；使用显式 DTO 和稳定错误码。
- SQL 必须参数化。表名、排序字段等无法参数化的部分必须来自封闭白名单。
- 涉及设备配额、投稿审核和一次性状态迁移的操作必须在事务中再次校验约束。
- API Key 只持久化安全摘要；密码使用适合密码的哈希算法，不得使用普通快速摘要替代。
- 配置通过环境变量或安全配置源注入，不得提交真实密钥、默认管理员密码或生产数据。

## 测试要求

- 领域规则必须有单元测试；修复缺陷时先添加能复现问题的测试。
- 多输入场景优先使用表驱动测试。只有测试彼此独立且不共享环境时才调用 `t.Parallel()`。
- 测试名称描述行为和结果，不绑定内部实现细节。
- HTTP 测试使用 `httptest`；数据库测试使用临时目录或独立测试数据库，不读写仓库中的 `data/`。
- 不得通过任意延时、降低断言或跳过测试掩盖竞态和不稳定行为。
- 涉及并发的变更应额外运行 `go test -race ./...`；涉及 API 兼容的变更应运行契约或响应快照测试。

## 变更纪律

- 只修改当前需求所需内容，保持差异小而可审查，不顺带重构无关代码。
- 每次变更后检查 `git diff`，确认没有密钥、运行数据、构建产物和无关环境文件。
- 不得在未获明确授权时提交、推送、改写 Git 历史或修改其他仓库。
- 无法验证的假设必须写入文档或最终报告，不得将推测表述为已完成事实。
